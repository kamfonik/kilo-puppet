#!/bin/bash

# This is a dummy script that can be used on testing nodes that don't actually need to be backed up.
# It exists so Puppet still has somethign to install, and the cronjob doesn't send out emails that
# it can't find the backup script.
